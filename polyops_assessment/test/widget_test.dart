// Widget-level integration tests require a fully configured DI graph (getIt)
// and platform channels (SQLite, connectivity_plus). These are covered by
// running the app directly. BLoC logic and domain rules are fully covered
// by unit tests in test/presentation/ and test/domain/.

void main() {}
